<p id="notice"><%= notice %></p>

<h1>Crafts</h1>

<table>
  <thead>
    <tr>
      <th>Pdtimg</th>
      <th>Pdtname</th>
      <th>Pdtcategory</th>
      <th>Pdtprice</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @crafts.each do |craft| %>
      <tr>
        <td><%= craft.pdtImg %></td>
        <td><%= craft.pdtName %></td>
        <td><%= craft.pdtCategory %></td>
        <td><%= craft.pdtPrice %></td>
        <td><%= link_to 'Show', craft %></td>
        <td><%= link_to 'Edit', edit_craft_path(craft) %></td>
        <td><%= link_to 'Destroy', craft, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Craft', new_craft_path %>
